# Audit record — Noocyada weeraha

- **Resource path:** `resources/naxwe/12-noocyada-weeraha.md`
- **Collection / family:** naxwe / primary grammar
- **Priority:** P2
- **Method:** repository-only, line-by-line SLS content audit
- **Audit status:** approved; conservative SLS cleanup applied and awaiting review
- **Audit started:** 2026-08-12
- **File size at audit start:** 277 lines; 1,599 words; 10,311 bytes
- **Resource SHA-256 at audit start:**
  `4baae5d5854cced3d5d6bc44d5c2c5d0082a0e0a696131454bdec40d40507cce`
- **Resource-text changes during audit:** none

## Target output model

This is a coherent primary-grammar chapter rather than an OCR transcript. It
has one H1, six H2 headings, six H3 headings, four H4 headings, 36 numbered-
content lines carrying 44 numbered example/reference tokens, one Markdown
table with seven data rows, no diagrams, and no links. It has no front matter,
exercises, scan debris, malformed table, or broken reading order.

Cleanup should preserve the complete sentence-type system: declaratives and
the `waxaa` construction; yes/no and full-answer questions; `ma`, `miyaa`,
`-maa`, `-ee`, and other interrogative forms; indirect questions; nominal and
verbal negation; negative questions; imperatives; and the full seven-person
optative paradigm with its negative forms. Every numbered-content line and all
seven table rows should remain. Cleanup should repair exact agreement,
position, classification, heading, and spelling errors; qualify source-only
derivations and absolute grammatical judgments; and add resolving links to the
maintained SLS chapters without introducing new paradigms or examples.

## Audit progress

| Original lines | Status | Finding |
| --- | --- | --- |
| 1-8 | reviewed | N12-R001 |
| 10-42 | reviewed | N12-R002 |
| 43-47 | reviewed | N12-R003 |
| 49-75 | reviewed | N12-R004 |
| 77-105 | reviewed | N12-R005 |
| 107-130 | reviewed | N12-R006 |
| 131-150 | reviewed | N12-R007 |
| 151-170 | reviewed | N12-R008, N12-R009 |
| 172-190 | reviewed | N12-R010 |
| 192-221 | reviewed | N12-R011, N12-R012 |
| 223-236 | reviewed | N12-R013 |
| 238-277 | reviewed | N12-R014 |
| whole file | reviewed | N12-R015 |

## Findings

| ID | Lines | Class / severity | Repository comparison | Proposed SLS action | Evidence label |
| --- | --- | --- | --- | --- | --- |
| N12-R001 | 1-8 | Valuable sentence-purpose introduction, but `Weer kastaa waxay` has broken agreement and the claim that every sentence is made from “the same lexical content” does not express the intended contrast / high | Masculine singular `weer` consistently takes `wuxuu` elsewhere; `ereyfur.md` supports `weer tebineed` and `weer weyddiimeed`, while later sections independently add negative and optative constructions | Retain the purpose-based orientation. Recast it as one lexical/propositional content being usable for different purposes, correct agreement, and make clear that the three opening purposes introduce rather than exhaust the later construction types. | `repository-supported`; `grammar-correction`; `logic-correction`; `intentional-retained` |
| N12-R002 | 10-42 | The `waxaa` transformation and all paired examples are central, but the prose puts the focused `baa/ayaa` OM after the verb although its own examples put it before; it categorically stars independently possible `Cali wuxuu aadayaa`; and unique `Waxuu` conflicts with its own `waxaa + uu → wuxuu` rule / fatal | Chapter 9 states that a `baa/ayaa`-marked OM precedes the verb and provides the exact subject-clitic order rules. Dictionary `waxaa` supports the particle and example `Waxaa yimid Xasan`; the naxwe collection has 228 instances of `wuxuu/Wuxuu` but only this one `Waxuu` | Retain examples 4-6, 10-17, every equivalence/arrow, and the source transformation. Correct the focused OM position to before the verb. Scope the postverbal-OM requirement to the intended `waxaa` counterpart and predicate/reading instead of declaring `Cali wuxuu aadayaa` intrinsically broken. Correct `Waxuu` to `Wuxuu`; link chapter 9. | `same-file-exact`; `repository-supported`; `logic-correction`; `judgment-qualification`; `orthographic-correction`; `intentional-retained` |
| N12-R003 | 43-47 | The processing motivation is unsupported and contradicts the preceding simple OMs: it says postposing is needed when the OM is structurally complex, though `Xamar` and `qalin` are not; the universal cross-language claim is outside repository evidence / high | No repository grammar independently supports the listener-processing or world-language universal; the same file demonstrates both simple and complex postverbal OMs | Retain a short constructional summary, but remove the unsupported universal and do not present complexity as a necessary condition. At most identify heavy/complex OMs as one environment the source highlights. | `same-file-exact`; `scope-correction`; `unsupported-generalization` |
| N12-R004 | 49-75 | The yes/no distinction, three `ma` examples, nominal-predicate question, and focus interpretations are useful; however, “always” and “never possible” judgments are established only by this source / medium | Chapter 16 independently preserves the `Ma Axmed baa hadiyad keenay?` pattern and contrasts interrogative `ma` with `miyaa`; `ereyfur.md` supports `qurub weyddiimeed`, `diirad`, and `khabar magaceed` but not the full categorical distribution | Retain every example and the source's focus contrast. Present the distribution and starred form as this chapter's constructional analysis, without silently promoting it to an exceptionless SLS-wide rule; link chapter 16. | `repository-supported` in part; `interpretive-qualification`; `intentional-retained` |
| N12-R005 | 77-105 | The `miyaa` section is repository-supported and should remain complete; its `ma + ayaa → miyaa` derivation and categorical ban on initial `miyaa` with nominal predicates are source analyses rather than independently established rules, and `Weli` remains undefined / medium | Dictionary `miyaa` gives `Wiilasha miyaa yimid?`, `Miyuu Cali yimid?`, and `Miyuusan Cali imaanayn?`; chapter 16 independently repeats the post-OM position and `miyuu` pattern. `Weli` recurs as a technical heading in chapters 9 and 11 but is absent from the grammar glossary | Retain examples 21, 23, 32, and 36, the positional contrast, the fusion with `-uu`, and the nominal-predicate example. Label the derivation and categorical word-order judgment as the source's analysis. Retain `Weli` with the same glossary-status note used in chapters 9 and 11. | `repository-supported`; `interpretive-qualification`; `unresolved`; `intentional-retained` |
| N12-R006 | 107-130 | The parent heading says all full-answer questions are `-ee` even though its first subsection is `-maa`; example 57's statement that `Cali` “precedes” does not identify a valid contrast, and example 58 incorrectly invokes subject/object interchange for temporal `goor`; the historical origin of `kuma/tuma` is unsupported / fatal | Dictionary supports interrogative `maxaa`, masculine `kuma`, feminine `tuma`, `yaa`, `immisa`, and `meeqa`, but not the proposed histories; chapter 9 supplies the relevant constituent-order/clitic rules. The displayed orders themselves are `Goormuu + Cali + verb` and `Cali + goormuu + verb` | Rename 12.2.3 for full-answer questions and keep `-maa`, `-ee`, other interrogatives, and indirect questions beneath it. Retain examples 53, 54, 57, and 58, but describe their actual order without calling `goor` a direct object. Preserve the `-maa`, `maxaa`, and `kuma/tuma` derivations explicitly as source analyses; retain the dictionary-supported forms. | `same-file-exact`; `repository-supported` in part; `heading-repair`; `label-correction`; `interpretive-qualification`; `intentional-retained` |
| N12-R007 | 131-150 | The examples and independent `kee/tee` use are valuable, but `-ee` is misclassified as a `tifaftire magac madaxeed`; the prose says `baa/ayaa` is not overt although example 66 overtly has `baa` and example 67 has fused `baad`; and the contraction account is source-only / fatal | Chapter 3 and `ereyfur.md` use `tifaftire weyddiimeed` for `-kee/-tee`; chapter 3 provides a complete masculine/feminine table. Same-file examples are `Xaggee baa...` and `Dugsigee baad...`, directly contradicting absence of a focus marker | Retain examples 66-67, the starred separation judgment, `Caasha xaggay ku nooshahay?`, and independent `kee/tee` examples. Correct the category to `tifaftire weyddiimeed`; explain that `-ee` marks the interrogative constituent while the shown constructions can also carry overt or fused `baa`. Label the contraction derivation as the source's account and link chapter 3. | `same-file-exact`; `repository-supported`; `classification-correction`; `logic-correction`; `interpretive-qualification`; `intentional-retained` |
| N12-R008 | 151-160 | `yaa`, `immisa`, and the remaining interrogatives broaden the chapter usefully, but `keligeed` has faulty agreement and `Buuggani immisuu joogaa?` asks price/value rather than presenting `immisa` as a direct object; the resulting universal object-focus claim is therefore unsupported / fatal | Dictionary defines `yaa` through `kuma/tuma`, and defines `immisa` as asking number, price, or time. It defines `meeqa` as `immisa`. No other chapter classifies the displayed price expression as a direct object | Retain all four displayed interrogative patterns. Correct the agreement around `yaa`; classify `immisa` in this example by its dictionary-supported price/value function rather than as a direct object, and remove the unsupported universal inferred from that label. | `repository-supported`; `grammar-correction`; `classification-correction`; `scope-correction`; `intentional-retained` |
| N12-R009 | 162-170 | The two indirect-question examples and nominal-head analysis are useful, but the claim that Somali has no interrogative words in dependent clauses and the comparison with many other languages exceed the evidence; the two examples demonstrate only the `halka/goorta` relative-nominal strategy / high | Chapter 11 supports relative/dependent clauses and their nominal heads. Dictionary `in²` independently gives indirect-question-like `Weyddii bal inay fog tahay`, showing the repository contains more than the two head-relative patterns displayed here | Retain examples 78-79 and analyze their actual `halka/goorta` strategy. Present it as one productive indirect-question construction, not proof that no other strategy exists; remove the unsupported cross-language contrast and link chapter 11. | `repository-supported`; `same-file-exact`; `scope-correction`; `unsupported-generalization`; `intentional-retained` |
| N12-R010 | 172-190 | Nominal-focus negation and examples 4-5 are valuable; the opening says negation can target only one constituent and that it is always focused, while the ban on separated `-aan` and `baa` is supported only by this source / medium | `ereyfur.md` supports `weer diidmo`, `diirad`, and `diiradeeye`; chapter 8 independently distinguishes negative particles by mood but does not establish this complete focus theory | Retain examples 4-5, the missing-`-uu` starred example, and the separated-marker starred example. Scope the one-constituent/focus account and absolute judgments to this chapter's analyzed constructions; link the maintained focus-order and negation resources. | `repository-supported` in part; `interpretive-qualification`; `intentional-retained` |
| N12-R011 | 192-212 | The verbal-negation examples, placement of `ma`, and selected ending contrast are important; `giraalka` is an exact typo, and the universal `-in` past / `-o` present-future, person-invariant claim conflicts with the repository's stative, auxiliary, and irregular paradigms / fatal | `ereyfur.md` and chapter 3 support `weer qiraal`; `giraal` occurs nowhere else. Chapter 8 limits person invariance to displayed `ma cunin` and gives `ma adki`, `ma adkid`, `ma ahayn`, `ma cuni doono`, and other counterpatterns | Retain examples 10-11, both optional clitic examples, and `Ciise shineemada igu ma arkin`. Correct `giraalka` to `qiraalka`; scope the ending/person summary to the regular selected forms actually shown and direct readers to chapter 8 for exceptions and larger paradigms. | `repository-supported`; `orthographic-correction`; `scope-correction`; `intentional-retained` |
| N12-R012 | 213-221 | The nominal-predicate affirmative/negative contrast is useful, but “cannot be negated directly” and the obligatory transformational analysis are source-specific / medium | Chapter 8 independently supports irregular `yahay → ma aha`; `ereyfur.md` supports `khabar magaceed`, but no other repository source requires the exact two-step derivation | Retain examples 23-25 and the equivalence. Describe `ma aha` as the negative counterpart and preserve the intermediate `waa yahay` derivation explicitly as this chapter's analysis rather than an exceptionless procedural rule. | `repository-supported` in part; `interpretive-qualification`; `intentional-retained` |
| N12-R013 | 223-236 | Negative-question coverage and all three examples are important; the last paragraph is difficult to read because “`-aan` only” refers to the negative marker while interrogative `ma/miyaa` is still present, and its evidence consists only of rejected double-`ma` forms / high | Dictionary `miyaa` independently provides `Miyuusan Cali imaanayn?`, confirming a fused interrogative-plus-negative construction; chapter 8 supports `yaan/yuusan/yaanay` in optatives but does not independently establish every negative-question focus rule | Retain examples 1-3 and both starred double-marker forms. Clarify that verbal-focus negative questions use the interrogative construction plus the negative `-aan` element/fusion and do not insert a second independent negative `ma`; present the detailed focus combinations as the source's analysis. | `repository-supported` in part; `clarity-correction`; `interpretive-qualification`; `intentional-retained` |
| N12-R014 | 238-277 | Imperative and optative sections are complete and useful. Imperative forms and negative `ha ... -in/-a` agree with chapter 8; the optative table's four `an/ad` cells conflict with chapter 7's maintained `aan/aad`, while its `ha sheegeen` parallels the supported `ha qoreen` and should remain / high | Chapter 7 gives the complete seven-person optative table with `aan qoro`, `aad qorto`, `aan qorno`, `aad qortaan`, and `ha qoreen`; chapter 8 independently supports `ha cunin/cunina` and `yaan/yuusan/yaanay`. `ereyfur.md` supports `hab talo` and `weer talo` | Retain examples 4-8 and 1-2, every table row, `ha sheegeen`, and the focus-particle observations as the chapter's analysis. Normalize only the four unsupported `an/ad` cells and following prose to `aan/aad`; retain and link the full imperative/optative/negative system in chapters 7-8. | `repository-supported`; `orthographic-normalization`; `interpretive-qualification`; `intentional-retained` |
| N12-R015 | whole file | The chapter has dense source numbering with gaps, resets, equivalences, combined `15/16`, and 44 numbered tokens on 36 entry lines; it has no navigation to the maintained prerequisite paradigms / medium | Chapters 3, 7, 8, 9, 11, and 16 plus `ereyfur.md` supply the maintained determiner, mood, negation, focus/order, dependent-clause, parallel-source, and terminology treatments | Preserve all 36 numbered-content lines, all 44 numbered tokens on those lines, every source gap/reset/equivalence, and all seven table rows. Add a source-numbering note and concise resolving links; add no invented examples, missing numbers, tables, or paradigms. | `structural-only`; `repository-supported`; `intentional-retained` |

## Proposed SLS-native blueprint

The cleaned chapter should retain its title and topic sequence:

1. purpose-based sentence types;
2. declaratives and the `waxaa` construction, with the corrected focus order;
3. yes/no questions with `ma` and `miyaa`;
4. full-answer questions with `-maa`, `-ee`, `yaa`, `immisa`, and related
   forms;
5. the demonstrated `halka/goorta` indirect-question strategy;
6. nominal-focus, verbal, and nominal-predicate negation;
7. negative questions;
8. affirmative and negative imperatives; and
9. the complete seven-person optative paradigm and its negative forms.

All 36 numbered-content lines, all 44 numbered tokens on those lines, and all
seven paradigm rows should remain with their source numbering, gaps, resets,
equivalences, and judgments. Cleanup should correct only the uniquely resolved
forms `Waxuu → Wuxuu`, `giraalka → qiraalka`, and four optative `an/ad →
aan/aad` forms, plus the identified agreement and analytical labels. Source-
only derivations (`waxaa`, `miyaa`, `-maa`, `kee/tee`) should remain clearly
attributed rather than deleted. No example or paradigm should be invented.

## Approval gate

- **Audit approval:** approved by the maintainer on 2026-08-12 with the
  instruction, "go ahead."
- **Approved finding IDs:** N12-R001 through N12-R015
- **Cleanup:** applied; awaiting maintainer review
- **Cleanup approval:** not started
- **Complete:** no
- **Decision requested:** review and approve the cleaned resource before the
  cleanup-approval or complete stages are marked.

## Audit validation

- Resource SHA-256 after audit:
  `4baae5d5854cced3d5d6bc44d5c2c5d0082a0e0a696131454bdec40d40507cce`
  (unchanged).
- Resource diff during this audit: none.
- Structure checked: one H1, six H2, six H3, four H4, one Markdown table with
  seven data rows, no fenced diagrams, and no links.
- Numbered content checked: 36 numbered-content lines, 44 numbered tokens on
  those lines, and 46 numbered example/reference tokens in the whole file.
- Existing local links: none; all proposed target files exist.
- Source analyses deliberately retained for qualification: the `waxaa`,
  `miyaa`, `-maa`, and `kee/tee` derivations; categorical focus judgments; and
  the technical heading `Weli`.

## Cleanup result and review

### Applied cleanup

- Recast the opening around one lexical/propositional content used for
  different purposes, repaired agreement, retained the three opening purposes,
  and previewed the later negative and optative constructions.
- Added a source-numbering note and preserved every source gap, reset,
  equivalence, arrow, and combined `15/16` label.
- Corrected the `baa/ayaa` focus position to before the verb and distinguished
  it from the postverbal OM of the displayed `waxaa` counterpart.
- Scoped the rejection of *Cali wuxuu aadayaa* to the intended `waxaa`
  correspondence, preserving the possibility of another independent reading.
- Corrected unique `Waxuu` to repository-supported `Wuxuu`; retained the
  source transformation, every paired example, and the `waxaa + uu → wuxuu`
  analysis.
- Removed the unsupported typological universal and limited the heavy-OM
  processing account to a source observation rather than a necessary
  condition.
- Retained technical source heading `Weli`, documented its repeated use in
  chapters 9, 11, and 12, and recorded its absence from the grammar glossary.
- Retained all `ma` and `miyaa` examples and focus readings while scoping
  absolute judgments and the `ma + ayaa → miyaa` derivation to the source's
  analysis.
- Renamed 12.2.3 for full-answer questions so its `-maa`, `-ee`, other-word,
  and indirect-question subsections all agree with the parent heading.
- Corrected the order descriptions for examples 57-58 without calling temporal
  *goor* a direct object; retained the `-maa`, `maxaa`, and `kuma/tuma`
  derivations as source analyses.
- Corrected `-ee/-kee/-tee` to **tifaftire weyddiimeed** and resolved the
  internal contradiction by identifying overt `baa` in example 66 and fused
  `baad` in example 67.
- Corrected agreement around `yaa`; reclassified *Buuggani immisuu joogaa?*
  by the dictionary-supported price/value function and removed the false
  direct-object generalization.
- Retained both indirect-question examples and their `halka/goorta` strategy,
  but removed the unsupported claim that they exhaust Somali possibilities and
  the unsupported cross-language comparison.
- Retained every nominal-focus and verbal-negation example and starred form;
  scoped the one-focus account to the constructions under analysis.
- Corrected `giraalka` to glossary-supported `qiraalka` and limited the
  `-in/-o` and person-invariance summary to the selected regular forms, with a
  link to stative, auxiliary, future, and irregular paradigms.
- Retained examples 23-25 and described `ma aha` as the supported negative
  counterpart while preserving the intermediate `waa yahay` derivation as the
  source's account.
- Clarified that negative questions combine the interrogative construction
  with negative `-aan` rather than inserting a second independent negative
  `ma`; retained all three examples and both starred double-marker forms.
- Retained every imperative and optative example and all seven table rows.
  Normalized the four unsupported `an/ad` cells to chapter 7's `aan/aad`, kept
  `ha sheegeen`, and preserved the `yaan/yuusan/yaanay` system.
- Added eight resolving link occurrences to seven maintained grammar and
  glossary targets.

### Deliberately retained

- All 36 numbered-content lines and all 44 numbered tokens on those lines,
  including every gap, reset, equivalence, arrow, and combined number.
- The complete seven-row optative table; only four approved `an/ad` spellings
  changed to `aan/aad`.
- Every declarative, interrogative, negative, negative-question, imperative,
  and optative topic and example.
- Every starred grammaticality form, with judgments qualified only where the
  audit identified an alternate reading or source-limited construction.
- Source analyses of `waxaa`, `miyaa`, `-maa`, `maxaa`, `kuma/tuma`, and
  `kee/tee`, explicitly attributed rather than silently removed.
- Technical source heading `Weli` and source optative `ha sheegeen` with their
  repository status documented.

### Cleanup validation

- `git diff --check`: passed.
- Numbered content: all 36 numbered-content lines and all 44 numbered tokens on
  those lines remain; no source number was added, removed, or renumbered.
- Table: the single table and all seven original person rows remain; no row was
  added, removed, or reordered.
- Structure: one H1, six H2 headings, six H3 headings, four H4 headings, one
  valid Markdown table, and no fenced diagram.
- Required repairs verified: `Waxuu`, `giraalka`, the false postverbal
  `baa/ayaa` focus statement, the `-ee`/focus-marker contradiction, the
  `layeele/yeele` label on temporal *goor*, and table forms `an/ad` are absent.
- Required retained forms verified: `Wuxuu Cali aadayaa Xamar`, `ha sheegeen`,
  all three negative questions, and both negative optative examples remain.
- Local links: eight occurrences checked across seven unique existing targets.
- Provenance TSV validation: every row has 10 tab-separated fields.
- Post-cleanup navigation follow-up: both glossary links now target the
  approved bilingual `ereyfur.md`; no grammar content changed.
- Cleaned file size: 336 lines; 2,044 words; 14,071 bytes.
- Cleaned SHA-256:
  `01e030505b4df600e292bc88770fa0255a2d5cb268437ebc525ff112920f390d`.
