# Audit record — Dhismaha oraah magaceedyada

- **Resource path:** `resources/naxwe/10-dhismaha-oraah-magaceedyada.md`
- **Collection / family:** naxwe / primary grammar
- **Priority:** P2
- **Method:** repository-only, line-by-line SLS content audit
- **Audit status:** approved; cleanup approved 2026-08-23; complete
- **Audit started:** 2026-08-12
- **File size at audit start:** 151 lines; 784 words; 5,150 bytes
- **Resource SHA-256 at audit start:**
  `31d0ae5912434268617a691a4ef8331f07fcccf5ec2c438dd0c6c50a41957491`
- **Resource-text changes during audit:** none

## Target output model

This is a compact, coherent primary-grammar chapter rather than an OCR
transcript. It has one H1, six H2 sections, no lower headings, 26 numbered
examples on 26 entry lines, 31 numbered references in total, four fenced tree
diagrams, no tables, and no links. It contains no front matter, exercises,
scan debris, malformed tables, or broken reading order.

Cleanup should preserve the full progression from a single nominal head and
determiners through head/modifier structure, possession, recursively embedded
modifiers, `ee`, and relative clauses. Every numbered example and all four
diagrams should remain. The cleanup should correct two uniquely supported form
errors, distinguish noun heads from adjective/relational modifiers, scope
grammaticality marks to the intended structure rather than every possible
reading of the word string, align the `ee` explanation with its own diagram,
and leave unsupported source-only forms unresolved.

## Audit progress

| Original lines | Status | Finding |
| --- | --- | --- |
| 1-18 | reviewed | N10-R001 |
| 19-41 | reviewed | N10-R002 |
| 43-58 | reviewed | N10-R003 |
| 60-69 | reviewed | N10-R004 |
| 71-82 | reviewed | N10-R005 |
| 84-102 | reviewed | N10-R006 |
| 104-123 | reviewed | N10-R007 |
| 124-140 | reviewed | N10-R008 |
| 142-151 | reviewed | N10-R009 |
| whole file | reviewed | N10-R010 |

## Findings

| ID | Lines | Class / severity | Repository comparison | Proposed SLS action | Evidence label |
| --- | --- | --- | --- | --- | --- |
| N10-R001 | 1-18 | Clean introduction and five useful one-head OM patterns; `lagu daran` uses the stative form where the dependent negative `lagu darin` is required, and defining OM only as sentence arguments is narrower than its later predicate uses / high | Dictionary `dar³` supports the transitive verb; repository dependent negatives use `-in`; cleaned chapter 9 defines OM as a form that may function as an argument; chapter 3 supplies the complete determiner order | Correct only `lagu daran` to `lagu darin`. Describe OM as a nominal phrase that can occupy sentence functions, including an argument or nominal predicate. Retain examples 1-5, their analyses, and the fixed-order claim with a link to chapter 3. | `repository-supported`; `form-correction`; `scope-correction`; `intentional-retained` |
| N10-R002 | 19-41 | The head/modifier distinction and first diagram are valuable, but `Soomaaliyeed` is grouped with noun `Maryan` as another noun, the head definition is grammatically malformed, and `Kani waa Maryan` is starred although the prose concedes it is grammatical with a different meaning / high | `ereyfur.md` distinguishes `madax` and `faahfaahiye`; dictionary and broad repository use support `Soomaaliyeed` as a relational modifier in phrases such as `Jaamacadda Soomaaliyeed`; the sentence `Kani waa Maryan` is a nominal-predicate construction under chapter 9 even though it no longer means “this is Maryan's book” | Retain examples 6-7, both deletion contrasts, and the complete diagram. Rename the section/model from strictly “two nouns” to head plus modifier, distinguish proper-name possessor `Maryan` from relational modifier `Soomaaliyeed`, repair the head definition, and remove the absolute asterisk from `Kani waa Maryan` while explaining that deletion destroys the intended possession reading. | `repository-supported`; `classification-correction`; `grammar-correction`; `judgment-qualification` |
| N10-R003 | 43-58 | Fronted-possessor construction and focus test preserve useful structure; `waydaariyo` conflicts with dictionary `weydaari`, and `Axmed baan gurigiisa arkay` may be grammatical under a split/different reading even if it does not preserve the single-OM analysis / high | Dictionary `weydaari` uniquely supports the spelling; chapter 9's focus rules permit focused `Axmed` as an independent OM; chapter 3 supports the possessive suffix in `gurigiisa/timirtiisa` | Correct `waydaariyo` to `weydaariyo`. Retain examples 12-13 and `Axmed gurigiisa baan arkay`; present the focus-insertion form as unavailable for the intended single-OM possession reading rather than absolutely ungrammatical under every interpretation. | `repository-supported`; `orthographic-correction`; `judgment-qualification`; `intentional-retained` |
| N10-R004 | 60-69 | Predicate constituency test and examples 17-18 are useful; the account again treats `Soomaaliyeed` as if it were a second noun and groups `-aad/-eed/-ood` without limiting the claim to relational uses / medium | Glossary `faahfaahiye` covers modifiers; chapter 3 distinguishes determiner and possessive categories; repository phrases repeatedly use relational `Soomaaliyeed`; `-aad` also has numeral/ordinal uses outside this construction | Retain examples 17-18, their judgments, and all three relational examples. Describe the suffixed forms as relational modifiers in this construction, and scope the non-head claim to that use rather than every occurrence of the strings `-aad/-eed/-ood`. | `repository-supported`; `classification-correction`; `scope-correction`; `intentional-retained` |
| N10-R005 | 71-82 | Recursive-embedding introduction is useful, but its claim about every language is unsupported, examples 24-26 are two-member building blocks rather than the promised three-element cases, `Agal` lacks canonical lexical support, and `kharribanyahay` is unique / high | The next examples 27-29 actually supply the three-element structures; chapter 11 supports embedding within an OM without establishing a universal-language claim; dictionary has `aqal` but no lexical `agal`; dictionary supports `kharriban` but repository evidence does not uniquely select joined or spaced `kharriban yahay` here | Retain the recursion topic and examples 24-26 as preparatory two-member patterns, not the three-element evidence. Scope the typological statement to the source analysis. Correct `Agal` to dictionary-supported `Aqal`; retain `kharribanyahay` explicitly unresolved and do not respell or respace it by inference. Retain `Abaalgudbixintu` as a source compound because no unique segmentation correction is established. | `repository-supported` in part; `scope-correction`; `classification-correction`; `lexical-correction`; `unresolved`; `intentional-retained` |
| N10-R006 | 84-102 | Examples 27-29 and the nested diagram clearly illustrate three successive elements, but `guud` and `Soomaaliyeed` function as modifiers rather than noun heads, so the repeated “three nouns” and M1/M2/M3 labels overstate their word class / high | Dictionary `guud` supports a qualifying meaning; repository use of `Soomaaliyeed` is relational; glossary distinguishes `madax` from `faahfaahiye`; the same-file diagram correctly represents the structural nesting independent of exact word class | Retain examples 27-29 and the complete nested diagram. Describe three successive elements/constituents rather than three nouns; preserve M1/M2/M3 only as source shorthand with a note that an M-position here may be filled by a relational/adjectival modifier. Keep the nested relations and do not add alternate parses. | `repository-supported`; `classification-correction`; `terminology-clarification`; `intentional-retained` |
| N10-R007 | 104-123 | The three starred strings usefully contrast the intended co-modifier structure with nesting, but at least `Dukaanka dharka Cali` can receive a nested reading in which `Cali` modifies `dharka`; therefore the strings are not necessarily ungrammatical without specifying the intended relation / high | The preceding diagram licenses M3 as a modifier of M2; the prose itself says the intended reading instead has both M2 and M3 directly modifying M1; chapter 6 gives `[Dukaanka kabaha ee Cali]` for the co-modifier reading | Retain examples 34-36, their asterisks, and the flat schematic, but explicitly state that the judgments reject the intended direct/co-modifier readings. Do not claim that every string is impossible under every nesting or lexical interpretation; link chapter 6's supported `ee` construction. | `same-file-exact`; `repository-supported`; `judgment-qualification`; `intentional-retained` |
| N10-R008 | 124-140 | All three `ee` repairs and the outer/inner OM diagram are useful, but prose says `ee` makes the two modifiers the same level while the diagram places `dukaanka dharka` as one inner OM and `ee Cali` as modifier of that whole OM / high | The same-file tree gives the exact constituency; chapter 6 independently has `[Dukaanka kabaha ee Cali]`; dictionary `ee¹` gives `Xarunta dhexe ee xisbiga` as a dependent-link example | Retain examples 38-40 and the complete diagram. Replace the same-level claim with the diagram's analysis: `ee` introduces a modifier attached to the already formed inner OM. Link chapter 6 and do not manufacture another tree. | `same-file-exact`; `repository-supported`; `logic-correction`; `intentional-retained` |
| N10-R009 | 142-151 | Three complete relative-clause modifiers close the chapter cleanly; the final future promise is stale now that chapter 11 exists / low | Chapter 11 defines `weer faahfaahineed`, analyzes omitted heads, and independently uses `Intii...` as a temporal nominal head; glossary supports the restrictive/appositive relative-sentence terms | Retain examples 42-44 unchanged. Replace the future promise with a direct link to chapter 11 and briefly identify the head-plus-relative-clause continuation without duplicating its paradigms. | `repository-supported`; `navigation-update`; `intentional-retained` |
| N10-R010 | whole file | Source numbering contains gaps, starts/stops across several series, and appears 31 times; four diagrams and the chapter's prerequisite/follow-on relations currently have no navigation / medium | Extant example numbers are 1-7, 12-13, 17-18, 24-29, 34-36, 38-40, and 42-44; chapters 3, 6, 9, and 11 plus the glossary provide maintained detail | Preserve all 26 numbered examples, all gaps and resets, and all four diagrams. Add a numbering note and concise resolving links; never fill missing numbers, renumber examples, add inferred tables, or replace diagrams with unsupported structures. | `structural-only`; `repository-supported`; `intentional-retained` |

## Proposed SLS-native blueprint

The cleaned chapter should retain its title and complete sequence:

1. one nominal head with determiners;
2. head plus possessor or relational modifier;
3. fronted possessor plus possessive-marked head;
4. relational-suffix modifiers and predicate constituency;
5. recursively nested OM structure, first with two-member building blocks and
   then three successive elements;
6. the contrast between nested and intended co-modifier readings;
7. `ee` introducing a modifier of an already formed OM; and
8. a relative clause serving as the modifier inside an OM.

All 26 examples and all four diagrams should remain with the original source
numbers and gaps. Cleanup should make only the supported `daran → darin`,
`waydaariyo → weydaariyo`, and `Agal → Aqal` form corrections; qualify
structural judgments and classifications without deleting their examples; and
retain `kharribanyahay` and `Abaalgudbixintu` as unresolved/source forms.

## Approval gate

- **Audit approval:** approved by the maintainer on 2026-08-12 with the
  instruction, "go ahead."
- **Approved finding IDs:** N10-R001 through N10-R010
- **Cleanup:** applied and approved 2026-08-23
- **Cleanup approval:** approved 2026-08-23
- **Complete:** yes
- **Decision requested:** review and approve the cleaned resource before the
  cleanup-approval or complete stages are marked.

## Audit validation

- Resource SHA-256 after audit:
  `31d0ae5912434268617a691a4ef8331f07fcccf5ec2c438dd0c6c50a41957491`
  (unchanged).
- Resource diff during this audit: none.
- Structure checked: one H1, six H2 sections, no lower headings, four fenced
  diagrams, and no Markdown tables.
- Numbered content checked: 26 examples on 26 entry lines and 31 numbered
  references in total; all source gaps recorded.
- Existing local links: none; all proposed target files exist.
- Source forms deliberately unresolved: `kharribanyahay` and
  `Abaalgudbixintu`.

## Cleanup result and review

### Applied cleanup

- Corrected dependent `lagu daran` to `lagu darin`, defined OM by its nominal
  head and possible sentence functions, and linked the maintained simple-
  sentence account.
- Added a source-numbering note and linked the complete determiner order in
  chapter 3.
- Renamed the two-noun section to head plus modifier, distinguished possessor
  `Maryan` from relational modifier `Soomaaliyeed`, repaired the malformed head
  definition, and retained `Kani waa Maryan` as grammatical with a different
  nominal-predicate meaning.
- Corrected `waydaariyo` to dictionary-supported `weydaariyo` and scoped
  `Axmed baan gurigiisa arkay` to a different/split reading rather than calling
  the word string absolutely ungrammatical.
- Scoped `-aad/-eed/-ood` to their relational uses in the displayed
  constructions and retained every relational example.
- Renamed the recursive section from three nouns to three constituents,
  identified examples 24-26 as two-member building blocks, and qualified the
  unsupported universal-language statement.
- Corrected `Agal naageed` to dictionary-supported `Aqal naageed`; retained
  `Abaalgudbixintu` and `kharribanyahay` with explicit source/unresolved notes.
- Clarified that `guud` and `Soomaaliyeed` fill modifier positions rather than
  proving that all three constituents are nouns; preserved the full nested
  diagram.
- Scoped the asterisks on examples 34-36 to the intended direct/co-modifier
  readings while acknowledging that an unlinked sequence may have a different
  nested reading.
- Reconciled the `ee` prose with its own diagram: the linker introduces a
  modifier of the already formed inner OM. The diagram remains and its outer
  branch label now states `ee + M`.
- Replaced the stale future promise with a direct link to chapter 11's relative-
  clause analysis.
- Added five resolving links to five maintained grammar/glossary targets.

### Deliberately retained

- All 26 numbered examples on their original 26 entry lines, with every number
  and gap. Only approved example 26 changed internally from `Agal` to `Aqal`.
- All four fenced diagrams; only the approved outer modifier label in the
  `ee` diagram was clarified.
- The complete topic sequence: determiners, head/modifier structure,
  possession, relational forms, recursive embedding, co-modifier readings,
  `ee`, and relative clauses.
- Source forms `Abaalgudbixintu` and `kharribanyahay` without inferred
  segmentation or spacing.
- Every possession, focus, predicate, `ee`, and relative-clause example.

### Cleanup validation

- `git diff --check`: passed.
- Numbered content: all 26 example entries and all original entry numbers and
  gaps remain; no example was added, removed, or renumbered.
- Diagram preservation: four opening and four closing fences remain, yielding
  all four original diagrams.
- Structure: one H1, six H2 sections, no lower headings, and no Markdown table.
- Required form corrections verified: `lagu darin`, `weydaariyo`, and
  `Aqal naageed` are present; the three rejected forms are absent.
- Unresolved/source forms verified: `kharribanyahay` and `Abaalgudbixintu`
  remain unchanged and have explicit notes.
- Local links: five occurrences across five unique existing targets; both
  requested heading anchors exist.
- Provenance TSV validation: every row has 10 tab-separated fields.
- Post-cleanup navigation follow-up: the glossary reference now targets the
  approved bilingual `ereyfur.md`; no grammar content changed.
- Cleaned file size: 175 lines; 978 words; 6,790 bytes.
- Cleaned SHA-256:
  `50aeee4eb1a6a50a07a723c019a26d8f60440ba2e3a51b7547a5a1bda20d0436`.
