# Audit record — Letter B: qaamuus

- **Resource path:** `resources/qaamuus/01-b.md`
- **Collection / family:** qaamuus / dictionary letter file (pilot)
- **Priority:** P2
- **Method:** repository-only, structural scan plus targeted line review
- **Audit status:** approved; dictionary cleanup applied and awaiting
  maintainer cleanup review
- **Audit started:** 2026-08-19
- **File size at audit start:** 3,225 lines; 39,478 words; 254,096 bytes
- **Resource SHA-256 at audit start:**
  `57fd94c721cb4f702d0df7c8e3304eeee2549aca1bf5ed9d7cbe60391b62c46d`
- **Resource-text changes during this audit:** none

## Target output model

This file is a dictionary letter slice, not a grammar chapter. It has one H1
(`# B`), **3,223** list entries (`- **` headword lines), and no per-file
provenance block.

This collection follows the **dictionary evidence** pattern: preserve headwords,
codes, definitions, and cross-references; fix uniquely supported OCR and
unreadable control characters; route substantive lexical disputes to `data/`.
Do not rewrite definitions or modernize Somali.

Cleanup should add a one-line collection note with a link to
[`00-sources.md`](../../../../resources/qaamuus/00-sources.md), repair the control
character and merged-entry line documented below, correct two domain-tag typos
with abbrev-table support, and leave the **`ą`** (U+0105) headword set and
wordlist orphan heads documented for a follow-up pass.

## Source and repository evidence

Cleaned registry files (`qaamuus/00-sources.md`, `00-abbreviations.md`,
`README.md`) define entry format, the **48,138**-entry recount rule, and
wordlists derivation.

Paired baseline: [`resources/madax-ereyo/01-b.md`](../../../../resources/madax-ereyo/01-b.md)
has **3,225** bare heads. Exact parity: **3,223** shared heads; **2** wordlist-only
orphans (`Baktiiriyada`, `baddelo`) traced to malformed prose fragments in
[`14-q.md`](../../../../resources/qaamuus/14-q.md) and
[`26-u.md`](../../../../resources/qaamuus/26-u.md), not to missing B-letter entries
here.

Repository scan (2026-08-19):

- zero replacement-character (`�`) lines;
- one STX control character (`\x02`) inside an `ld` target (line 3158);
- one merged list item (line 124);
- **40** headwords using **`ą`** (Latin a with ogonek), plus **22** further
  in-body `bą` references — invalid in Somali orthography, parallel to existing
  `aa`-spelled homograph sets (`baad¹`/`²` vs `bąad¹`/`²`, `baahi¹`/`²` vs
  `bąahi`, etc.);
- intentional homonym duplicate **`ballaarin`** (lines 718–719): geometry sense
  vs nominalisation of **`ballaari**;
- domain tags **`(baai.)`** (line 641) and **`(c.daaw.)`** (line 1812) vs
  abbrev-table keys **`baay.`** and **`daaw.`**.

## Audit progress

| Original lines | Status | Finding |
| --- | --- | --- |
| 1 | reviewed | QAB01-R001 |
| 2-3225 (structure) | reviewed | QAB01-R002 |
| 51-793, 425, 547 (`ą` heads) | reviewed | QAB01-R003 |
| 3158 | reviewed | QAB01-R004 |
| 124 | reviewed | QAB01-R005 |
| 641, 1812 | reviewed | QAB01-R006 |
| 718-719 | reviewed | QAB01-R007 |
| wordlists parity | reviewed | QAB01-R008 |
| 4, 40, 482, 1623 (typography) | reviewed | QAB01-R009 |
| whole file | reviewed | QAB01-R010 |

## Findings

| ID | Lines | Class / severity | Repository comparison | Proposed SLS action | Evidence label |
| --- | --- | --- | --- | --- | --- |
| QAB01-R001 | 1 | Single H1 only; no collection framing / low | Cleaned erey-bixin and registry files use a one-line note plus `00-sources.md` link | Keep `# B`. Add one Somali sentence that this is the B-letter dictionary slice, with a local link to [`00-sources.md`](../../../../resources/qaamuus/00-sources.md). Do not add compiler/year here. | `repository-supported`; `navigation-update`; `intentional-retained` |
| QAB01-R002 | 2-3225 | **3,223** entries in consistent `- **` dictionary format; alphabetical by headword; no foreign letter sections / low | Sample matches cleaned README entry pattern; no OCR page markers | Retain one H1 and list format. Do not re-sort entries. Do not add definitions. | `structural-only`; `repository-supported`; `intentional-retained` |
| QAB01-R003 | 51-793, 425, 547 | **40** headwords used invalid **`ą`** glyph / medium | Applied 2026-08-19: `ą`→`a` with homonym allocation; **`bąd`→`bed⁴`**, **`bąh`→`bah²`**; collection-wide second pass for **`Ą`**, **`Wąa`**, paradigm **`aą`**, broken `ld` hyphen targets | `repository-supported`; `encoding-correction`; `applied` |
| QAB01-R004 | 3158 | STX control character inside `ld buunshabbaxsanaansho` / high | Line 3159 head **`buunshabbaxsanaansho`** exists | Remove the control character so the `ld` target reads `buunshabbaxsanaansho`. Do not change the definition text. | `repository-supported`; `form-correction` |
| QAB01-R005 | 124 | Two entries merged on one line: **`baajin`** and **`baajis`** / medium | Standard one-entry-per-line pattern throughout file | Split into two list items. Retain both definitions and the mutual `ld` cross-reference. | `structural-only`; `repository-supported` |
| QAB01-R006 | 641, 1812 | Domain tags **`(baai.)`** and **`(c.daaw.)`** not in abbrev table; **`baay.`** and **`daaw.`** are / low | [`00-abbreviations.md`](../../../../resources/qaamuus/00-abbreviations.md); entries otherwise readable | Correct `(baai.)` → `(baay.)` on **`bakteeriafayj`**. Correct `(c.daaw.)` → `(daaw.)` on **`beeryaro²`**. Do not alter definition prose. | `repository-supported`; `form-correction` |
| QAB01-R007 | 718-719 | Two **`ballaarin`** heads: geometry (`xis.`) vs nominalisation of **`ballaari** / low | Both senses distinct; superscripts not used because part-of-speech codes differ | Retain both entries. Do not merge. | `repository-supported`; `intentional-retained` |
| QAB01-R008 | wordlists parity | Wordlist has **2** heads absent here: **`Baktiiriyada`**, **`baddelo`** — extraction orphans from other letter files / medium | [`14-q.md`](../../../../resources/qaamuus/14-q.md) line 897; [`26-u.md`](../../../../resources/qaamuus/26-u.md) line 259; wordlists `01-b` lines 3226–3227 | No qaamuus change. Record for paired [`madax-ereyo/01-b.md`](../../../../resources/madax-ereyo/01-b.md) audit: remove or relocate orphans after upstream prose-fragment cleanup in `14-q` / `26-u`. | `repository-supported`; `cross-collection`; `unresolved` |
| QAB01-R009 | 4, 40, 482, 1623 | Missing space after parenthetical codes, e.g. `(nax.)Mid`, `(f.g)Wax` — source typography / low | Same pattern occurs across letter files | Retain as printed unless a later collection-wide typography pass is approved. Do not mass-insert spaces in this pilot. | `intentional-retained` |
| QAB01-R010 | whole file | Valid B-letter pilot with a small set of supported OCR repairs and a large deferred **`ą`** batch / medium | This audit establishes the dictionary letter-file cleanup template | Preserve entry inventory except approved fixes. Link file-review folder in collection note. Do not mark the letter file complete. | `structural-only`; `repository-supported`; `intentional-retained` |

## Proposed SLS-native blueprint

The cleaned file should retain:

1. `# B` plus a one-line registry link;
2. **3,223** dictionary entries in existing order;
3. repaired line 124 split, line 3158 control character, and two domain tags;
4. deferred **`ą`** headwords unchanged pending homonym map;
5. both **`ballaarin`** entries.

Wordlist orphan removal belongs in the wordlists pass, not here.

## Approval gate

- **Audit approval:** approved by the maintainer on 2026-08-19 with the
  instruction, "go ahead."
- **Approved finding IDs:** QAB01-R001 through QAB01-R010 except **`ą`**
  normalization (QAB01-R003 deferred as approved)
- **Cleanup:** applied; awaiting maintainer review
- **Cleanup approval:** not started
- **Complete:** no

## Audit validation

- Resource SHA-256 after audit:
  `57fd94c721cb4f702d0df7c8e3304eeee2549aca1bf5ed9d7cbe60391b62c46d`
  (unchanged).
- Resource diff during this audit: none.
- Structure checked: one H1, 3,223 `- **` entries, no replacement characters.

## Cleanup result and review

### Applied cleanup

- Added Somali collection note with link to [`00-sources.md`](../../../../resources/qaamuus/00-sources.md).
- Split merged **`baajin`** / **`baajis`** line into two entries.
- Removed STX control character from **`buunshabbaxsanaan`** `ld` target.
- Corrected `(baai.)` → `(baay.)` and `(c.daaw.)` → `(daaw.)`.

### Deliberately retained

- Retained all **40** former **`ą`** heads as normalized **`aa`/`a` spellings with homonym numbers** (QAB01-R003 applied 2026-08-19).
- Both **`ballaarin`** entries.
- Parenthetical typography without inserted spaces (QAB01-R009).
- All definitions and cross-reference wording except the control-character target.

### Deferred to follow-up

- ~~Systematic **`ą` → `aa`** normalization with homonym renumbering.~~
  **Applied 2026-08-19** — collection-wide `ą`/`Ą` glyph normalization
  (`ą`→`a`, homonym allocation, manual `bąd`→`bed⁴`, `bąh`→`bah²`); see
  correction-log batch row QAB01-R003.
- Wordlist orphan heads in other letter files (08-s, 12-g, 14-q, 15-k, 17-m,
  18-n, 22-a) — pre-existing extraction fragments; fix during each letter audit.

### Cleanup validation

- `git diff --check`: passed.
- Entry count: **3,224** (`- **` lines after split); 3,228 lines total.
- Cleaned SHA-256:
  `5a7df1a999c5d812cb728cb799daba9961c63dddb616c01da5616e1287ec954d`.
